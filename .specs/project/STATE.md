# State — API OBM

## Decisions

- D1: OBM = **Ontologia Brasileira de Medicamentos** (not "Observatorio") — corrected across all files
- D2: Import is always full-replace (`DROP SCHEMA` + recreate) — no incremental updates
- D3: Meilisearch indexes only VMP/AMP/Supplier (expand later)
- D4: SQL dump too large for GitHub (137MB > 100MB limit) — removed from git, documented as separate download
- D5: Streaming pipeline via `io.Pipe` — converted SQL never touches disk (except `--convert-only`)
- D6: ZIP extraction requires temp file (Go `archive/zip` needs `io.ReaderAt`)
- D7: Domain slugs use hyphens (e.g. `legal-category`) not underscores
- D8: No single transaction for import — autocommit per statement (same as `psql -f`)
- D9: `obm_metadata` as simple key-value table (not a separate schema)

## Blockers

- `gh` CLI not authenticated — cannot create GitHub release via CLI (manual web UI or `gh auth login` needed)
- `migrations/postgres/001_obm_schema.sql` lost during filter-branch — will be regenerated by import tool

## Todos

- Authenticate `gh` CLI to create GitHub releases programmatically
- Add integration test for full import pipeline with real dump
- Consider `migrations/postgres/001_obm_schema.sql` regeneration via `--convert-only --output`

## Deferred

- Expanding Meilisearch indexes beyond VMP/AMP/Supplier → v1.2.0
- Scheduled/automatic imports → v1.2.0
- Downloading from portal URL (access is restricted) → no ETA
- CSV/XML/JSON input formats → no ETA

## Lessons

- `git filter-branch` to remove large files works but nukes untracked files in `migrations/` — always back up
- HeidiSQL dumps have MySQL-specific syntax that needs careful regex conversion (bigint unsigned, tinyint(1), INSERT IGNORE, etc.)
- pgx pool is shared across the app — import CLI should create its own pool to avoid connection exhaustion

## Preferences

- Docs in Portuguese (PT-BR) for user-facing content
- Commit messages in English
- CLI flags use `--kebab-case`
