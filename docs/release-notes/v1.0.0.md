# Release Notes — v1.0.0

**Data:** 24/05/2026
**Status:** Released

## API OBM — Ontologia Brasileira de Medicamentos

Release inicial da API REST para consulta de dados da [Ontologia Brasileira de Medicamentos (OBM)](https://portal-obm.saude.gov.br/), seguindo o modelo dm+d (Dictionary of Medicine and Devices) do NHS adaptado para o Brasil.

---

### Funcionalidades

#### Endpoints de consulta

| Entidade | Endpoints | Destaques |
|----------|-----------|-----------|
| **VMP** | Listagem, por ID, detalhado | Detalhe inclui VTM, ingredientes, rotas, formas, classes ATC, catmats |
| **AMP** | Listagem, por ID, detalhado | Detalhe inclui VMP, fornecedor, ingredientes, rotas, condicoes de preservacao |
| **VTM** | Listagem, por ID | Principios ativos genericos |
| **VMPP** | Listagem, por ID | Apresentacoes virtuais (quantidade) |
| **AMPP** | Listagem, por ID | Embalagens comerciais com codigo EAN-13 |
| **DCB** | Listagem, por ID | Denominacoes Comuns Brasileiras |
| **Ingredientes** | Listagem, por ID | Substancias ativas com codigo CAS e DCB |
| **Fornecedores** | Listagem, por ID | Fabricantes com CNPJ e codigo de pais |
| **Dominios** | Listagem por tipo, por tipo e ID | 32 tipos de classificacao (forma, via, ATC, controle, etc.) |

#### Busca global

- Busca full-text via Meilisearch em VMPs, AMPs e Fornecedores
- Filtros por nome, codigo, fabricante, descricao e status ativo
- Filtro por tipo de entidade (`vmp`, `amp`, `supplier`)
- Relevancia score nos resultados

#### Autenticacao

- Login via `/auth/login` com username/senha
- Token JWT (HS256) com expiracao configuravel (padrao: 24h)
- Middleware de autenticacao em todos os endpoints `/api/v1/`
- Compatibilidade com formato `Bearer <token>` e token direto (Swagger UI)

#### Paginacao

- Todos os endpoints de listagem usam paginacao cursor-based
- Limite configuravel (padrao: 20, maximo: 100)
- Cursor codificado em base64 para paginacao eficiente

#### Infraestrutura

- Docker Compose com PostgreSQL 16, Meilisearch v1.8 e API
- Health check publico em `/health`
- Reindexacao administrativa via `POST /api/v1/admin/reindex`
- Reindexacao automatica ao iniciar (`SYNC_ON_STARTUP=true`)
- Graceful shutdown com timeout de 5 segundos

#### Documentacao e ferramentas

- Swagger UI integrado em `/swagger/index.html`
- Collection Postman com ambiente local pre-configurado
- README.md com documentacao completa para usuarios
- Guia de teste (TESTING.md)

---

### Correcoes incluidas

- Descricao corrigida de "Observatorio de Medicamentos" para "Ontologia Brasileira de Medicamentos" (conforme portal oficial)
- Auth middleware aceita token com ou sem prefixo Bearer para compatibilidade com Swagger UI
- Porta do servidor alterada de 8080 para 8094
- Porta do PostgreSQL no host alterada de 5432 para 5433 (evita conflito com PostgreSQL local)
- Healthcheck do Meilisearch corrigido para usar curl em vez de wget
- Chave master do Meilisearch com tamanho minimo de 16 bytes
- Syntax SQL corrigida na migration de inicializacao
- Seed script corrigido para carregar .env via godotenv

---

### Stack

| Componente | Tecnologia |
|------------|------------|
| Linguagem | Go 1.25 |
| Framework HTTP | Gin v1.12 |
| PostgreSQL | pgx/v5 v5.9 |
| Busca | Meilisearch Go SDK v0.36 |
| JWT | golang-jwt/jwt/v5 v5.3 |
| Senhas | golang.org/x/crypto (bcrypt) |
| Docs | swaggo/swag v1.16 |
| Banco | PostgreSQL 16 Alpine |
| Search Engine | Meilisearch v1.8 |

---

### Dominios disponiveis

`form`, `route`, `flavour`, `legal-category`, `licensing-authority`, `avail-restriction`, `med-class`, `anvs-class`, `atc-class`, `control-drug`, `df-indicator`, `discontinued-ind`, `pres-status`, `non-avail`, `basis-of-name`, `basis-of-strnth`, `brimunologico`, `catmat`, `country`, `unit-of-measure`, `package`, `ont-form-route`, `preserv-cond`, `rename-comp`, `ingredient-source`, `healthcare-prof`, `indicacao-farmpop`, `monitoring-reason`, `name-change-reason`, `lic-auth-change-reason`, `phpid`, `local-aplicacao`

---

### Como atualizar

```bash
git pull origin main
docker compose up --build -d
```

Usuarios de seed:

| Username | Senha |
|----------|-------|
| `admin` | `admin123` |
| `viewer` | `viewer123` |
